/**
 * Wagmi configuration placeholder.
 * In production this would be configured with RainbowKit + Wagmi
 * to handle real wallet connections.
 *
 * For now, the app uses mock wallet context (WalletContext.jsx).
 */

export const SUPPORTED_CHAINS = ['Ethereum', 'Polygon', 'Base'];
export const ALCHEMY_NETWORK = 'eth-mainnet';
